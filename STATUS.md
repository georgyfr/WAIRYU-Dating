# STATUS — Suivi de l'avancement du projet Wairyu

> Tableau de bord mis à jour à chaque fin d'étape. Une étape n'est marquée « Terminée » qu'après validation de sa porte (gate).

| # | Étape | Statut | Date de fin | Gate validée |
|---|-------|--------|-------------|--------------|
| 0 | Cadrage & décisions fondatrices | ✅ Terminée | 2026-09-23 | ☑ |
| 1 | Socle technique & infrastructure Cloudflare | ✅ Terminée | 2026-09-23 | ☑ |
| 2 | Authentification & comptes | ✅ Terminée | 2026-09-22 | ☑ (test mobile fondateur restant) |
| 3 | Profils & photos protégées | ✅ Terminée | 2026-09-23 | ☑ (test mobile fondateur restant) |
| 4 | Questionnaire progressif & moteur de matching | ✅ Terminée | 2026-09-23 | ☑ (score ≥ 85 sur réponses identiques, deal-breaker exclu, latence feed < 300 ms) |
| 5 | Découverte dual-mode (Classique & Invisible) | ✅ Terminée | 2026-09-23 | ☑ (match Classique E2E, handshake E2E, bascule §4.8 sans perte) |
| 6 | Chat temps réel & révélation | ✅ Terminée | 2026-09-23 | ☑ (WS E2E 10/10, voice note, révélation E2E refus→accord, unmatch+blocage, re-floutage vérifié ; push VAPID prêt — clés à poser + test mobile restants) |
| 7 | Sécurité & modération | ✅ Terminée | 2026-09-23 | ☑ (smoke 56/56 : scam BLOQUÉ par règles, signalement→sanction E2E, badge vérifié visible, 2FA TOTP admin, régression 35/24/33/68/58 verte) |
| 8 | Monétisation préparée (éteinte) | ⏳ En attente | — | ☐ |
| 9 | Qualité, tests & beta fermée | ⏳ En attente | — | ☐ |
| 10 | Lancement francophonie | ⏳ En attente | — | ☐ |
| 11 | Post-lancement : épaississement & montée en charge | ⏳ En attente | — | ☐ |

## URLs en service

| Environnement | URL | Usage |
|---|---|---|
| **Production** | https://wairyu.wairyu.workers.dev | API + PWA (déploiements validés uniquement) |
| Staging | https://wairyu-staging.wairyu.workers.dev | Tests avant chaque montée en prod |

## Journal des livraisons

| Date | Livraison | Contenu |
|------|-----------|---------|
| — | Initialisation du dépôt | Spécification v0.1 + Analyse approfondie + Plan de réalisation 12 étapes |
| — | Décision d'architecture n°9 : stockage sans carte bancaire | R2 reporté (activation exige une carte) → **Cloudinary plan gratuit** pour photos + voice notes (assets authentifiés, URLs signées, module `StorageService` interchangeable) |
| 2026-09-23 | Sondes Cloudinary V1-V8 | Plan Free validé en réel (25 GB) : upload `authenticated` images+audio ✅, accès sans signature bloqué ✅, URL signée Worker-side ✅, flou par transformation ✅. Découverte : expiration `v=timestamp` non appliquée → sécurité déléguée à l'autorisation Worker. Voir `docs/STOCKAGE-CLOUDINARY.md` |
| 2026-09-23 | **Étape 0 — Cadrage** | Périmètre MoSCoW, 9 décisions validées (Décision 9 révisée), limites free tier relevées à J0, CGU + Politique de confidentialité + Registre RGPD v1, marque visuelle v1. Gate 0 ☑ |
| 2026-09-23 | **Étape 1 — Socle technique** | Monorepo TS strict (API Hono + PWA React/Vite + shared), D1 prod/staging provisionnées et migrées (users, sessions, rate_limits, metrics_daily), KV ×2, DO SQLite `ChatRoom`, cron purge, secrets posés, cookies signés HMAC, métriques d'usage (`/admin/usage`), smoke tests verts en prod. Gate 1 ☑ |
| 2026-09-22 | **Étape 2 — Authentification & comptes** | OTP email 6 chiffres (hashé, TTL 10 min, 3 essais, cooldown 60 s), Turnstile (widget créé, strict en prod), sessions 30 j glissants révocables (logout/logout-all), rate limiting D1 (IP + email), Google OAuth préparé (PKCE + fusion par email), RGPD : export JSON + suppression immédiate + trace anonyme J+30, écrans PWA mobile-first (18+ obligatoire), `/admin/*` protégé par jeton. 18 smoke tests + parcours navigateur réels. Voir `etapes/etape-02-authentication/` |
| 2026-09-23 | **Étape 2-bis — Connexions sociales Google + Facebook** (demande fondateur) | Boutons « Continuer avec Google / Facebook » sur inscription + connexion (actifs dès pose des secrets, sans redéploiement), OAuth complet Facebook (Graph v21.0, state signé, appsecret_proof), fusion de comptes par email vérifié, table `oauth_identities` (0004, prod+staging), callback Meta « Data Deletion Request » conforme + page `/data-deletion`, consentement 18+/CGU exigé avant inscription sociale, cas Facebook sans email géré. Smoke social 6/6 staging + 6/6 prod, smoke complet 18/18 (zéro régression). Guides d'activation Google §7.2 / Meta §7.3 dans `AUTHENTICATION.md` |
| 2026-09-23 | **Activation Brevo + Google** (clés fournies par le fondateur) | Guide pas-à-pas `docs/GUIDE-ACTIVATION-CLES.md` poussé ; secrets posés prod+staging (BREVO_API_KEY, EMAIL_FROM, GOOGLE_CLIENT_ID/SECRET) ; expéditeur Gmail validé chez Brevo ; test d'envoi réel OK ; OTP staging parti par email (`channel: "email"`) ; config prod : `emailProvider: "brevo"` + `googleEnabled: true` ; `/start` 302 accepté par Google (aucune erreur redirect/client). Reste : app Meta (bloc C) + tests mobiles fondateur |
| 2026-09-23 | **Incident redirect_uri_mismatch réglé — Google validé de bout en bout** | Premier clic fondateur : Erreur 400 redirect_uri_mismatch (URI de redirection absente du client OAuth Google ; faux négatif du test curl initial — ne suivait pas les redirections). Test technique reproductible mis au point (curl -L + cookies + UA). Correction fondateur dans la console (2 URIs enregistrées) ; test technique : plus d'erreur, écran de connexion servi avec nom « wairyu » ; capture fondateur : écran compte connecté via Google (`wairyu26@gmail.com`, badge Email vérifié). Connexion Google **validée de bout en bout** ✅ |
| 2026-09-23 | **Activation Facebook (Bloc C) — les 3 méthodes de connexion sont opérationnelles** | Récap config fondateur vérifié (App ID 3696…, Lifestyle, privacy policy, data deletion, 2 URIs) ; secrets posés prod+staging ; `facebookEnabled: true` ; `/start` 302 validé ; callback Data Deletion testé avec signed_request conforme à la spec Meta (signature sur la partie encodée, ex. PHP officiel) → contrat respecté. Reste : clic réel fondateur + test mobile Gate 2 |
| 2026-09-23 | **Incident « Invalid Scopes: email » réglé — parcours Facebook complet déployé** | Premier clic fondateur : le dialogue Meta refuse le scope `email` (permission non accordable aux apps récentes, écran bloquant pour les développeurs). Correctif : le dialogue ne demande plus que `public_profile` (secret `FACEBOOK_SCOPES` pour surcharger sans redéploiement) ; quand l'email n'est pas exposé, nouveau parcours **rattrapage OTP** : cookie signé 15 min + écran `#/fb-complete` (email + code à 6 chiffres, 18+/CGU) + `POST /api/auth/facebook/link` (garde-fou 409 si identité déjà reliée). Déployé prod+staging ; config/start/link/data-deletion re-testés verts ; contrat Meta Data Deletion inchangé. Reste : clic réel fondateur |
| 2026-09-23 | **Étape 3 — Profils & photos protégées** | Assistant profil 6 étapes (identité, orientation + consentement explicite dédié, ville + géoloc ≈11 km, bio 150, 3 prompts sur bibliothèque, photos, préférences + mode avec écran explicatif) ; pipeline photo client→Worker→Cloudinary (recadrage 4:5, WebP ~1024 px, upload signé serveur, max 6) ; URLs de livraison signées à la volée avec autorisation par photo (Invisible : nette refusée 403 tant que pas de révélation) ; flou CSS, rien de stocké de flouté ; RGPD étendu (export section profil, suppression = purge Cloudinary + D1) ; bug corrigé : transformation Cloudinary absente du chemin des URLs signées (401) ; outil `POST /admin/test-session` staging-only pour les smoke tests ; smoke étape 3 **35/35** vertes staging + vérifs prod. Voir `etapes/etape-03-profils/` |
| 2026-09-23 | **Incident « Turnstile Bloqué » — UX de récupération déployée** | Premier parcours Facebook complet du fondateur : l'OAuth Meta passe (compte confirmé) mais le widget Turnstile de l'écran `#/fb-complete` affiche « Vérification… Bloqué » sans moyen de relancer — blocage côté client par le moteur de risque Cloudflare (VPN/extensions/réseau ; config widget vérifiée correcte : managed, domaines prod/staging/localhost). Correctif UX : le composant partagé `Turnstile` capture le code d'erreur, active le retry auto Turnstile (3 s), affiche un message d'aide + bouton **« Réessayer la vérification »** (re-rendu propre du widget). Validé en réel sur staging avec navigateur headless (bloqué comme prévu → bouton apparu → re-rendu OK). Déployé staging (c8f7c01e) + prod (53815b2b), zéro changement de props pour les 3 écrans |
| 2026-09-23 | **Profil enrichi (demande fondateur) — date complète, localisation auto, intentions étendues** | (1) Naissance jour/mois/année en 3 listes (années récentes d'abord, jours ajustés au calendrier, âge EXACT 18+ serveur) : `users.birth_date` ISO + `birth_year` toujours synchronisé (invariant API) ; (2) « Où vis-tu ? » : bouton **Détecter ma position** → GPS navigateur → géocodage inverse **Nominatim** via Worker `GET /api/geo/reverse` (session requise, rate limit 30/h, cache KV ~1,1 km, UA politique OSM) → pays/ville/quartier remplis et modifiables — testé réel : `Douala III / Cameroun / Songbikako` ; (3) Intentions : + Vie de couple, Mariage, Rencontre interraciale (étapes 2 et 6, CHECK DB retirés — évolutif sans migration). Migrations 0006/0007 ; smoke régression **35/35** + smoke évolutions **20/20** (scripts/smoke_etape3b.sh) ; déployé staging+prod (54e55458) |
| 2026-09-23 | **3e mode de découverte « Mode interracial »** (clarification fondateur) | Précision du fondateur : l'« interracial » n'est pas une intention mais un **mode** de découverte dédié aux rencontres entre personnes de races, cultures et **continents différents**. Correction : `DiscoveryMode` étendu à `classic | invisible | interracial` (shared → API → front), « Rencontre interraciale » **retirée des intentions** (0 donnée impactée, vérifié prod+staging), 3e carte « Mode interracial » sur l'écran des modes (texte : rencontres au-delà des frontières, autres continents) + pied de page vitrine mis à jour ; migration **0008** (motif zéro perte, table feuille) : CHECK sur `mode_default` retiré → tout futur mode = zéro migration ; comportement photos du mode = classic (nettes) en attendant le matching inter-continents de l'Étape 4. Smoke **24/24** staging (mode interracial accepté/relu, intention interracial → 400, mode inconnu → 400) ; compteurs prod avant/après identiques (1/3/2/3/0) ; déployé staging (1518293a) + prod (e9e8157a) |
| 2026-09-23 | **Étape 4 — Questionnaire progressif & moteur de matching** | (1) Banque de questions **versionnée** en D1 (`q_items`, seed 12 N1 « Essentiel » + 18 N2 « Personnalité » en français, dimensions values/goals/communication/personality/attachment) + réponses 1 écriture/réponse (`q_answers`, upsert, reprise garantie) ; (2) écran Questionnaire (#/questionnaire) : une question à la fois, barre de progression par niveau, récompense **« Ma personnalité »** rule-based à chaque niveau fini ; (3) **moteur de score pur** (shared/matching.ts) : dimensions pondérées renormalisées MVP (valeurs .26, objectifs .21, communication .16, personnalité .16, attachement .11, préférences .10), **deal-breakers binaires exclusifs** (exclusion de la paire, pas un malus), tolérance ±2 stable par paire/jour (« pas un verdict ») ; (4) **« Pourquoi ce match ? »** : 2 forces + 1 vigilance + 2 sujets de conversation déduits des écarts ; (5) **feed scoré** GET /api/feed : 1 requête pool (LEFT JOIN, zéro N+1, LIMIT 300) → filtres durs (actif, ≥1 photo, âge exact via birth_date, genres, intention, activité 30 j) → distance haversine **SAUTÉE en Mode interracial (portée mondiale — la matérialisation produit du mode du fondateur)** → scoring → top 20 paginé, photo principale signée (floue si le propriétaire est Invisible). Écran Découvrir (#/discover) minimal (le swipe dual-mode = Étape 5) + entrées depuis le compte. Smoke étape 4 **33/33** (Gate 4 : score ≥ 85 sur réponses identiques, deal-breaker exclu, latence feed < 300 ms, mode interracial mondial) + régression 35/35 & 24/24 ; déployé staging (cc603f68) + prod (421a9f86, migration 0009 : 30 questions semées, données intactes) |
| 2026-09-23 | **Incident migration 0006 réglé — leçon D1 + Time Travel** | La version initiale de 0006 (`PRAGMA foreign_keys=OFF` + `DROP TABLE users`) a été **cascadée par D1** (le PRAGMA n'y est pas honorer) : sur staging, 4 sessions de test vidées. Sur prod, appliquée par erreur en séquence avec 0007 : 3 sessions / 2 photos / 3 prompts effacés → **prod restaurée intégralement via D1 Time Travel** (bookmark 05:50 UTC, dernier écrit réel 05:44), 0006 réécrite en motif **zéro perte** (sauvegarde des 4 tables enfants → DROP enfants d'abord → DROP users → rename → recréation + restauration), ré-appliquée : compteurs prod strictement identiques (1/3/2/3/0/2), nouvelles colonnes en place. Règle archivée : **jamais de DROP d'une table parente sur D1 sans avoir supprimé/recréé les enfants autour, et comptage avant/après obligatoire** |
| 2026-09-23 | **Perf — chargement long corrigé : police Nunito auto-hébergée + cache immutable** | Diagnostic (curl timings + endpoints authentifiés via session de test) : API rapide (me 150 ms, q 200 ms, feed interne 130 ms), bundle brotli OK → coupable = **CSS Google Fonts render-blocking** dans `<head>` (page blanche tant que fonts.googleapis.com ne répond pas — lent/instable sur certains FAI dont le Cameroun) + assets revalidés à chaque visite (`max-age=0`). Correctifs : (1) **Nunito auto-hébergée** — 2 woff2 variables (latin complet FR, 56 KB) dans `public/fonts/`, `@font-face` + `font-display: swap` (texte affiché immédiatement en police système), preload HTML de la graisse principale, **zéro requête tiers** dans le chemin critique (meilleure confidentialité aussi) ; (2) fichier **`_headers`** (Workers Assets) : `Cache-Control: immutable 1 an` sur `/assets/*` (hash Vite) et `/fonts/*` (suffixe -v1) → visites répètes sans revalidation. Smoke régression 24/24 + 33/33 staging ; prod vérifiée (health, 0 référence Google, polices/JS en cache immutable, compteurs D1 intacts 1/2/3) ; déployé staging (11a764e1) + prod (9f2f394c) |
| 2026-09-23 | **Étape 4-ter — Types de profils recherchés + navigation questionnaire (demande fondateur)** | « Quand il valide sa personnalité, lui montrer les types de profils compatibles, qu'il les sélectionne → ces types mis en avant dans ses matchs (avec les autres critères exigeants) ; et ajouter les boutons Sortir/Retour annoncés mais absents. » Livré : (1) **sélecteur « Les personnalités qui te correspondent »** affiché dès la validation du type (récompense N1, raffinement N2, Mon compte) : les 8 archétypes triés par affinité (forte → bonne → à découvrir, le sien inclus), **suggestion pré-cochée = affinités fortes**, ajustable, plafond **4 types**, bouton « Valider mes choix ✓ », modifiable à tout moment ; (2) API `PUT /api/personality/preferences` (validation stricte de chaque id, déduplication, plafond, 400 si pas de personnalité validée ; rate 60/h) + `GET /api/personality.prefTypes` ; migration **0011** (`personality_profiles.pref_types`, ALTER TABLE zéro perte, défaut `[]` = comportement 4-bis inchangé) ; (3) **feed : mise en avant réelle** — type ∈ sélection ⇒ affinité FORTE forcée dans la 6ᵉ dimension + raison « Son type fait partie de ceux que tu cherches » + champ `personalitySought` + chip vert « Type recherché ✓ » sur la carte Découvrir ; **priorité, jamais un filtre** (les autres profils restent visibles ; deal-breakers/valeurs/distance inchangés) ; re-priorisation dynamique vérifiée (changer la sélection change le feed) ; (4) **navigation questionnaire** : bouton **‹ Retour** = question précédente avec réponse PRÉ-REMPLIE et modifiable (correction d'erreur sans tout refaire, re-réponse sans réafficher la récompense), bouton **Sortir** = quitter à tout moment (tout est déjà enregistré, reprise à la 1re question sans réponse), compteur « question X/Y », libellé « Mettre à jour ma réponse » sur les questions revues. Smoke **prefs 26/26** (garde-fous 400, plafond, dédoublonnage, sought true/false, strong forcé, re-priorisation, RGPD) + régression **24/24, 33/33, 31/31** ; 1 bug rattrapé au smoke (GET sans colonne pref_types) ; déployé staging (c59efd65) + prod (fb941233, migration 0011 appliquée, compteurs intacts 1/2/30/1) |
| 2026-09-23 | **Étape 5 — Découverte dual-mode (Classique & Invisible)** | Gate 5 validée. (1) **Mode Classique** : pile de cartes mobile-first (swipe tactile pointer events, seuil 90 px, tampons ♥/✕), boutons Passe/Super/Like/**Rewind** (1/jour, annule match+conversation si créés), match mutuel → conversation `classic` → écran « C'est un match ! », filtres de base inline (âge, distance, montre-moi, intention) ; (2) **Mode Invisible** : cartes floutées enrichies (score, archétype, bio, **prompts désormais remplis**, **extraits de questionnaire partagés** « comme toi : … »), pas de swipe binaire — **« Demander à discuter »** (handshake : accepter/passer, boîte de réception + suivi envoyées, **double Discuter = match immédiat**, décliné non renvoyable — index unique anti-harcèlement) ; (3) **Limites gratuites** : 50 likes/j, 10 demandes/j, 1 super/j, 1 rewind/j — compteurs dans la table `rate_limits` existante (fenêtre UTC, purge cron), **tentative refusée ne consomme rien**, super pré-vérifié avant likes, barre de quotas front ; (4) **Passerelle §4.4** : « Proposer de passer en Invisible » sur une conversation Classique → acceptation = re-floutage pour les DEUX (historique conservé), refus = rien ne change ; écran **Mes matchs** (#/matches) avec mode de conversation + actions ; (5) **Conflits §4.6** documentés dans le code (flou piloté par le mode du propriétaire ; mode de la conversation décidé par l'action fondatrice ; bascule §4.8 ne touche jamais aux matchs) ; (6) **Top Compatibilité** : cron quotidien → top 5/user matérialisé (`top_matches`, batch D1, idempotent, hors quota) + `GET /api/discover/top` avec fallback à la demande + bandeau du jour ; **refactor central** `lib/discovery.ts` (une fonction feed = cron = top). Migration **0012** (6 tables feuilles sans CHECK). Smoke **68/68** + régression **24/24, 33/33, 31/31, 26/26** ; déployé staging (8934a541) + prod (0b8c8060), compteurs intacts (1/2/30/1) |
| 2026-09-23 | **Incident « push GitHub silencieusement raté » réglé** | Vérification fondateur via nouveau PAT : GitHub était resté à l'Étape 4-bis — le push de l'Étape 5 n'était jamais parti (remote sans ref de tracking). Push effectué (80d61cf), remote reconfiguré avec credential persistant → tout futur push fiable |
| 2026-09-23 | **Étape 6 — Chat temps réel & révélation** | Gate 6 validée par E2E. (1) **DO `ChatRoom`** : 1 conversation = 1 DO SQLite (messages seq monotone, reads, meta) + **WebSocket hibernation** par participant (tags userId) — protocole hello/ready, msg, typing, read, presence, system ; anti-spam 30 msg/min ; historique paginé par curseur (plan 6.1/6.3) ; (2) **tickets HMAC WS** (TTL 120 s) : auth sans cookie unifiant navigateur/mobile/tests ; (3) **écran chat unifié §4.9** (`#/chat/:id`) : bulles ✓/✓✓, emojis rapides, **voice notes** MediaRecorder→Cloudinary authentifié (URLs signées, garde MIME audio/*), présence + saisie, reconnexion auto ; (4) **compteurs de révélation** (≥ 15 messages ET ≥ 7 jours, DO + D1) et **flux de révélation §4.5** complet : demande (double confirmation front) → consentement explicite de l'autre → déblocage mutuel `revealed_at` → **anti-capture honnête** (filigrane prénom+date + message éducatif) → **feedback post-révélation** (Continuer/Ami/Pas pour moi, jamais montré) ; refus = rien ne change ; (5) **unmatch propre + blocage 1 clic** : sortie des deux côtés, re-floutage implicite (routes 404), re-match possible avec historique conservé (§4.8), exclusion bidirectionnelle des blocs du pool ; (6) **Web Push VAPID 100 % Workers sans dépendance** (RFC 8291/8292 via crypto.subtle) : nouveau message hors conversation active, nouveau match ×3 chemins, révélation — **dégradation gracieuse sans clés** (`enabled:false`), SW minimal + section Notifications dans Mon compte. Migration **0013** (revealed_at, revelations, reveal_feedback, blocks, push_subscriptions). Smoke dédié **58/58** dont sonde WS Node **10/10** + régression **24/24, 33/33, 31/31, 26/26, 68/68** ; 6 bugs rattrapés par les tests (SqlStorage `?N`/bindings, message perdu à l'upgrade, présence offline, `.bind()` sans `.run()`, re-match après unmatch, garde MIME) ; déployé staging (3edd61a9) + prod (8358de25, compteurs intacts). Reste : pose des clés VAPID + test push mobile fondateur |
| 2026-09-23 | **Étape 7 — Sécurité & modération** | Gate 7 validée. (1) **Vérification selfie semi-manuelle** : 3 poses aléatoires imposées (mélange crypto, re-générées à chaque reprise — anti photo papier), upload Cloudinary authentifié signé Worker, file `verification_requests`, review backoffice (URLs signées), badge « Identité vérifiée » (users.verified_at) sur profil/feed/matchs/chat ; (2) **modération automatique 0 neuron** : score de risque 0-100 par message DANS le DO avant livraison (haine FR/EN, menaces, drogues, scam : liens, crypto, argent, contacts externes, numéros) avec normalisation anti-leet/accents/répétitions ; ≥ 35 = livré + flag, ≥ 70 = **BLOQUÉ** (ni persisté ni diffusé), file `moderation_flags` (excerpt 120, best-effort waitUntil) ; (3) **signalement** : 7 motifs validés, **blocage mutuel immédiat + unmatch** (DO shutdown), re-like impossible tant que le blocage existe (validTarget 403) ; (4) **backoffice** : files vérifications/signalements (avec contexte DO ≤ 200 messages)/flags/check-ins, actions dismiss/warn/**suspend 1-30 j (403 API daté, sessions conservées)**/ban (sessions révoquées)/unban, **audit_admin** complet ; **2FA TOTP RFC 6238 100 % Workers** (setup/activate/disable, ±1 pas, temps constant) — activable en 1 clic, alors tout /admin/* exige X-Admin-TOTP ; (5) **check-in sécurité** « je vois X le [date] » + rappel push cron « Ça s'est bien passé ? » + file flagged ; (6) **anti-fraude** : multi-comptes par empreinte (ip_hash+user_agent_hash des sessions Étape 2, zéro donnée nouvelle) ; (7) **confidentialité v1** : pause (masqué + swipe 403), incognito (masqué sauf likes envoyés), visibilité du mode (flou toujours actif) — filtres dans le pool unique discovery.ts ; /api/me enrichi (verified, suspendedUntil). Migration **0014** (6 ALTER users + 5 tables feuilles sans CHECK). Smoke dédié **56/56** + régression **35/24/33/68/58** ; 4 bugs rattrapés (alias SQL mode_visible → feed 500, catch-22 2FA disable, DTO verified, latence réplication D1 + IPv6) ; déployé staging (3a18b6dc) + prod (9e046a26, compteurs intacts) |
| 2026-09-23 | **Navigation « dating app » classique + pages Messages & Mon profil** (demande fondateur) | L'app adopte la structure standard des apps de rencontre : (1) **barre d'onglets permanente** en bas — 🔥 Découverte · ⚡ Matchs · 💬 Messages (badge non-lus 9+) · 👤 Profil — visible sur les 4 pages principales ; les écrans secondaires (chat, questionnaire, assistant profil, paramètres) gardent leur bouton retour ; (2) **page Messages** (#/messages) : boîte de réception de TOUTES les conversations actives — aperçu du dernier message (« Vous : … », « 🎤 Message vocal »), heure relative, compteur de non-lus, photo floutée selon le MODE DE LA CONVERSATION (§4.8), état vide avec CTA découverte ; nouvelle API **GET /api/chat/conversations** (une entrée par match actif : résumé DO **/summary** = dernier message extrait + non-lus `COUNT(sender≠moi ET seq>last_read)`, photo signée, tri par activité) + polling léger 30 s pour le badge (règle `chatListUser` 240/h) ; (3) **page Mon profil** (#/myprofile) : TON profil comme le voient les autres — carte photo 4:5 navigable, âge exact, lieu, intention, bio, 3 prompts, badge personnalité + raffinement, résumé des préférences, accès rapides (modifier / questionnaire / paramètres) ; (4) **Compte requalifié « Paramètres & compte »** (retour vers Mon profil, entrée messagerie) ; (5) landing post-connexion → **Découverte** (l'accueil d'une app de rencontre) ; retour du chat → Messages. Zéro migration, zéro secret. Smoke dédié **smoke_pages.sh 34/34** (match like mutuel E2E, aperçu exact, non-lus, tri par activité, badge total) + régression **33/26/68/58/56** ; au passage : **chaîne de smoke dépolluée et fiabilisée** (purge de 13 comptes de test résiduels qui faussaient le check distance étape 4, noms de profils uniques par run, nettoyage RGPD systématique, boucle d'empreinte anti rotation d'IP du bac de test — les « flops multi-comptes » étaient la rotation d'IP) ; déployé staging (26a81013) + prod (8a1b4194), push GitHub 7c45e57..86bcf24 |
| 2026-09-23 | **Design responsive — mobile · tablette · PC** (demande fondateur : « gérer l'interface selon le mobile, la tablette et le pc ») | L'app reste mobile-first (référence), étendue par **deux paliers min-width sans aucune régression en dessous** : (1) **Tablette ≥ 640 px** — contenus élargis (680 px), cartes auth 480 px, CTA et boutons sociaux côte à côte, pile de swipe 420 px avec boutons plus généreux, **chat en fenêtre arrondie** (fini l'edge-to-edge) ; (2) **PC ≥ 1024 px** — la **barre d'onglets devient une sidebar verticale permanente** (mêmes boutons/badge non-lus, marque wairyu ajoutée dans TabBar, masquée < 1024 px), fond décoratif doux fixe, largeurs de lecture généreuses (Découverte 800, Matchs 780, Messages 740, Paramètres 760), colonne de pilotage Découverte centrée, **grille 2 colonnes pour les profils Invisible**, **page Mon profil en 2 volets** (carte photo collante à gauche + personnalité/préférences/actions à droite, grille explicite avec `:has()` et dégradation propre), bulles de chat contenues à 58 %, palier ≥ 1440 px pour borner la respiration ; (3) **3 bugs de layout rattrapés au passage** : chat qui héritait `align-items:center` de `.app` (en-tête/composer rétrécis au contenu — visible aussi sur mobile) → `stretch` + hauteur exacte `calc(100dvh − padding shell)` (fini le micro-scroll de 72 px), pages qui se resserraient sur contenu clairsemé (shrink-to-fit, ex. Messages ~400 px pour 1 conversation) → `width:100%`, et chevauchement titre/bouton « Mes matchs » sur 360-420 px (absolu → flex en ligne) ; (4) accessibilité : focus-visible homogène, respect de `prefers-reduced-motion`. Vérifié par **captures d'écran réelles à 390×844, 834×1112 et 1440×900** (staging, session de test observateur + match E2E temporaire supprimé ensuite) sur 9 écrans (accueil, signup, Découverte, Matchs, Messages, chat, Mon profil, Paramètres, questionnaire). Zéro changement API, zéro migration. Smoke **pages 34/34** + régressions **33/33, 68/68, 58/58, 56/56** (1er run étape 7 : rotation d'IP du bac documentée, vert au re-run) ; déployé staging (6b7fb066) + prod (61c3dc93) |

| 2026-09-24 | **Enrichissement « dating » des 3 modes de découverte** (demande fondateur : « la page classique doit être beaucoup enrichie… Tinder, Badoo… la page d'invisibilité… pareil pour les rencontres interraciales ») | Livré par mode : **Classique** = carrousel photos multi-images façon Tinder (zones de tap, flèches PC, points, compteur, +N), overlay dégradé sur la photo (nom, âge, badge vérifié, présence « En ligne/aujourd'hui », distance, drapeau), **« Tu plais ! »** — likes reçus en attente (fonction Tinder-Gold rendue **gratuite**, like en retour = match immédiat, passe discret), écran « C'est un match ! » à **deux photos** + CTA message, raccourcis clavier PC (← → ↑ R) ; **Invisible** = encart pédagogique 3 étapes, rituel rappelé sur chaque carte (15 messages · 7 jours · révélation à deux), badge « Photo floutée par choix », extraits partagés en citations, CTA strip adapté ; **Interracial** = drapeaux + continents (~80 pays FR/EN), badge « ✈️ Intercontinentale »/« Même pays », distance mondiale formatée, tri « d'abord les autres continents », note sécurité voyage. API : `FeedProfile` + photos(≤3 signées)/photoCount/online/distanceKm (last_seen dans la sous-requête candidats — feed 83 ms inchangé), **GET /api/discover/likes** (garde pause/blocages, photos groupées anti N+1, rate 60/h) ; doSwipe/doRequest acceptent des cibles hors deck. Zéro migration, zéro secret. Smoke **306/306** (pages 34, e4 33, e5 68, e6 58, e7 56, perso 31, prefs 26) ; E2E navigateur 3 modes + match modal (sessions profils virtuels, données test nettoyées : unmatch + DELETE D1) ; déployé staging (4871a486) + prod (be2fc7cb), push GitHub 232c290..bc69c15 |
| 2026-09-24 | **Interface Classique façon Tinder/Badoo — Task 30** (demande fondateur : « se rapprocher à 80 % de l'interface de référence ; garder l'existant, ajouter ce qui manque ») | Livré : (1) **Pile de swipe enrichie** — 3 cartes empilées visibles, drag X+Y (pointer events) avec rotation, **tampons texte LIKE / NOPE / SUPER**, sortie vers le HAUT pour le Super Like, vol depuis la position de drag (pas de retour élastique), barres de progression photos **façon Stories** (remplacent les points) ; (2) **Barre d'actions 5 boutons** — Rewind · ✕ · ✶ · ♥ · **⚡ Boost** (aperçu Wairyu+ offert : modale de confirmation, compte à rebours 30 min local, bouton pulsant tant qu'actif — honnêteté : marqué « aperçu ») ; (3) **2 nouveaux onglets** (barre = 6 onglets) — **✨ Likes** (#/likes) : grille des likes reçus façon référence, vignettes floutées + verrou, révélation par appui **100 % gratuite** (spéc. §6, pas de paywall), like en retour = match immédiat, mode Invisible = demande « Discuter » ; **🎬 Moments** (#/moments) : teaser + 4 cartes d'événements (défi photo hebdo, speed-friending Invisible, soirée IRL Douala/Yaoundé, live interculturel) avec « 🔔 Me prévenir » persisté localement et mention claire « aperçu de la prochaine mise à jour » ; (4) **3 modales** — **Filtres** (sliders âge min/max + distance live, chips montre-moi/intention, **switch « Profils vérifiés uniquement »** filtrant la pile localement, inoffensif serveur), **Notifications** (cloche + badge = likes reçus + supers + matchs < 7 j + demandes Invisible ; cache SWR partagé avec la page Matchs), **Détail profil** (clic sur le corps de carte → grand carrousel + toutes les infos + actions ✕/✶/♥) ; (5) en-tête modernisé (marque w + cloche) ; strip « Tu plais ! » conservée + lien « Voir tout » ; **BUG PRÉEXISTANT CORRIGÉ** : chaque swipe SAUTAIT un profil (removeFromDeck décalait le tableau PUIS advance incrémentait idx — actions enregistrées sur la mauvaise carte, vérifié en D1) → animation de sortie sur l'ancienne carte puis décalage seul ; Esc ferme toute modale. Zéro migration, zéro secret, zéro changement d'API. Smoke **306/306** ; E2E navigateur mobile 390 px + PC 1440 px (drag LIKE/NOPE/SUPER, modales, grille Likes avec verrou/révélation, Moments, badge cloche, match à deux photos, clavier) ; captures `captures/t30/` ; comptes de test supprimés (DELETE /api/account) ; déployé staging ×4 (39321ac9) — **prod après feu vert du fondateur** |

## Accès & comptes

| Service | État | Note |
|---|---|---|
| GitHub (georgyfr/WAIRYU-Dating) | ✅ Actif | Token fine-grained neuf vérifié (admin). ⚠️ Rappel permanent : révoquer/régénérer tout token partagé en clair |
| Cloudflare — Workers, D1, KV, Durable Objects, Turnstile | ✅ Vérifié | Sous-domaine `wairyu` ; URLs : `wairyu.wairyu.workers.dev` (prod), `wairyu-staging.…` (staging) |
| Cloudflare — R2 | ⏸️ Reporté | Activation impossible sans carte bancaire → Cloudinary le remplace (Décision 9) |
| Facebook Login | ✅ **Actif** (2026-09-23) | Secrets posés prod+staging ; `/start` 302 validé (dialogue v21.0, scope `public_profile` — Meta refuse `email` sur les apps récentes) ; parcours sans email exposé = rattrapage OTP (`#/fb-complete` + `/auth/facebook/link`) ; callback Data Deletion testé avec signed_request conforme Meta : contrat respecté (200 + url + confirmation_code) |
| Cloudinary (photos/voice notes) | ✅ Actif — **photos réelles Étape 3** | Cloud `nm7lozr4`, plan Free 25 GB, assets `authenticated` ; upload signé Worker-side (client→Worker→CDN), URLs de livraison signées à la volée, transformations vignette 200/flou 400 validées en production ; destroy testé (photo supprimée / compte supprimé → 404) |
| Cloudflare Turnstile (widget `wairyu-auth`) | ✅ Actif | Site key publique + secret posés (prod/staging) ; strict en production, sauté en staging |
| Brevo (emails OTP) | ✅ **Actif** (2026-09-23) | Clé API + expéditeur `wairyu26@gmail.com` validé chez Brevo ; test d'envoi réel OK ; `emailProvider: "brevo"` en prod et staging. Guide : `docs/GUIDE-ACTIVATION-CLES.md` bloc A |
| Google OAuth | ✅ **Actif** (2026-09-23) | Secrets posés prod+staging ; `/start` → 302 validé ; Google accepte client_id + redirect_uri (aucune erreur) ; bouton actif dans le front. Guide : `docs/GUIDE-ACTIVATION-CLES.md` bloc B |
